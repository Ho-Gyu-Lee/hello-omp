# Local smoke-test deployment targets
.tmp-omp-test*/
.tmp omp test*/

# Backup files produced by setup scripts when targeting a repo-local config dir
*.bak

# omp state if PI_CODING_AGENT_DIR is accidentally pointed at the repo
agent.db*
.DS_Store
